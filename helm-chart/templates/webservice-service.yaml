apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-webservice
spec:
  type: {{ .Values.webservice.service.type }}
  ports:
    - name: http
      port: 80
      targetPort: 5000
  selector:
    app: {{ .Release.Name }}-webservice